method: grid  # grid, bayes, random
metric:
  name: avg_tip_open_loop_rmse
  goal: minimize
parameters:
  model:
    values: ['gnn']
  dataset_folder:
    values: ['data/long_rollouts_100_max_amplitude_80_policy_harmonic_inputs']
  num_epochs:
    values: [2000]
  weight_decay:
    values: [1e-4, 1e-5, 1e-6]
  