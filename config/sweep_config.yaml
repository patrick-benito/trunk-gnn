method: grid  # grid, bayes, random
metric:
  name: test_loss
  goal: minimize
parameters:
  model:
    values: ['gnn']
  dataset_folder:
    values: ['data/overfit-mn-1e-1-short']
  node_hidden_features:
    values: [50]
  weight_decay:
    values: [1e-5]