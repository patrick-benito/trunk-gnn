method: grid  # grid, bayes, random
metric:
  name: test_loss
  goal: minimize
parameters:
  model:
    values: ['mlp']
  dataset_folder:
    values: ['data/mass_500g_harmonic']
  node_hidden_features:
    values: [50]
  shuffle:
    values: [false]