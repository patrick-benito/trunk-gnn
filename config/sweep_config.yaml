method: grid  # grid, bayes, random
metric:
  name: test_loss
  goal: minimize
parameters:
  model:
    values: ['gnn']
  dataset_folder:
    values: ['data/mass_100g_harmonic','data/no_mass_harmonic']
  use_edge_mlp:
    values: [True, False]