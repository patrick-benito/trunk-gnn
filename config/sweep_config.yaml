method: grid  # grid, bayes, random
metric:
  name: avg_tip_open_loop_rmse
  goal: minimize
parameters:
  model:
    values: ['gnn']
  dataset_folder:
    values: ['data/overfit-mn-1e1-short-05-dt']
  num_epochs:
    values: [2000]
  dt:
    values: [0.05]
  